diff --git a/blink.ino b/blink.ino
index e69de29..a1b2c3d 100644
--- a/blink.ino
+++ b/blink.ino
@@ -1,10 +1,12 @@
-// Starter sketch — fill in the blanks during the workshop.
-// Goal: blink the built-in LED on pin 13 once per second.
+const int LED_PIN = 13;

 void setup() {
-  // TODO: set pin 13 as an OUTPUT
+  pinMode(LED_PIN, OUTPUT);
 }

 void loop() {
-  // TODO: turn the LED on, wait, turn it off, wait
+  digitalWrite(LED_PIN, HIGH);
+  delay(1000);
+  digitalWrite(LED_PIN, LOW);
+  delay(1000);
 }
